# DEV_OVERVIEW

## 项目背景

本项目用于记录和维护当前用户级 Codex harness。其管理范围包括用户级
`AGENTS.md`、skills、插件，以及其他可由 Codex 用户配置的文件。

项目通过 Git 保留 harness 的版本历史，使同一套用户级 Codex 配置可以长期保存、
共同维护，并迁移到不同设备。项目的权威远程仓库为
<https://github.com/XZ135/codex-harness.git>。

## 当前目标与开发方向

- 建立当前设备上用户级 Codex harness 的可追踪仓库基线。
- 在用户明确要求时，检查当前设备的用户级 Codex harness，识别新增或变化的配置，
  同时结合当前安装版本与最新官方 Codex 文档识别兼容性变化，并将确认需要维护的内容
  更新到本仓库。
- 让仓库内容可用于不同设备之间的配置迁移和协同维护。

## 全局原则与约束

- 仓库维护以用户明确发起的检查或更新要求为触发条件，不将本方向解释为后台自动监控授权。
- 应覆盖 Codex 的用户级可配置内容；具体纳入版本控制的文件仍需考虑敏感信息、
  设备专属状态、缓存和可移植性。
- 只纳管可迁移、由用户维护且会被 Codex 消费的声明式配置；插件目录、安装状态、
  版本号和远程 ID 等运行时或派生快照不作为长期维护文件。插件启用配置仍随
  `config.toml` 纳管，实际安装状态由目标设备上的 Codex 插件管理命令确认。
- 官方版本新增的能力只有在当前 harness 已配置、为保持既有工作流所必需，或经用户确认
  需要启用时才纳入；不得仅因 Codex 提供新配置项就写入默认值或提前启用实验能力。
- 每次 harness 更新形成提交后，应将提交同步到权威远程仓库，避免本地与远程版本长期分叉。
- 配置更新应保留清晰的版本历史，以支持跨设备迁移、共同维护和必要的版本回溯。

## 中长期方向

- 持续补充新出现或此前尚未纳管的用户级 Codex 配置类型。
- 逐步明确可移植配置、设备特有配置和不应入库内容之间的边界。

## 关键未知与待决策事项

- 待首次盘点后确认具体同步目录、敏感信息排除规则，以及跨设备存在差异时的合并策略。
- 待确认是否需要在人工触发之外提供自动化的检查、安装或迁移工具。

## 子文档索引

当前无子级方向文档。
